<h1>Blog Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.title, "blog_posts/#{post.id}" %> 
      <!--original: blog_post_path(post) -->
    </li>
  <% end %>
</ul>

<%= link_to "New", new_blog_post_path %>

